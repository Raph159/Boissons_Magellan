# Set IP route API + kiosk
/etc/nginx/sites-available/boissons

# Deploy project after push github just run ./deploy.sh




# Dont forget to reload nginx after any modif
# => sudo nginx -t && sudo systemctl reload nginx
